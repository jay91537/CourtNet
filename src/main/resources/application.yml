spring:
  # DB ?? ?? ??
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/courtnetdb
    username: root
    password: js365@dwy5
  # JPA ??
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  servlet:
    session:
      tracking-modes: cookie